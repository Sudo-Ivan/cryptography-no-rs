# Cryptography-no-rs

This is a fork of the `cryptography` package that removes ALL Rust dependencies.

Do NOT use this package in production, it is still very much a work in progress and I have no idea if I will even finish it.
